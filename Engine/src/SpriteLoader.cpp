#include <string>
#include "SpriteLoader.h"

using namespace Hiage;
using namespace std;

SpriteDescriptor SpriteLoader::LoadResource(string filename, string name) const
{
    return SpriteDescriptor();
}
